# Initial stage - Scaffolding

Create a directory to host the workshop sources and change into it

```sh
mkdir -p gaudi-example 
cd gaudi-example
```

Create the initial directory structure with

```sh
gaudi -s .
```
